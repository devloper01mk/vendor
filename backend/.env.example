DATABASE_URL="postgresql://postgres:root@localhost:5432/vendor_expense?schema=public"
# Start without Postgres (only /health and process smoke-test; DB routes will error)
# SKIP_DATABASE=true
JWT_SECRET="change-me-in-production-min-32-chars"
JWT_REFRESH_SECRET="change-me-refresh"
PORT=4000
UPLOAD_DIR="./storage/invoices"
PUBLIC_BASE_URL="http://localhost:4000"
# Google Sign-In (OAuth 2.0 — Gmail / Google account)
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
GOOGLE_CALLBACK_URL="http://localhost:4000/auth/google/callback"
# Web app URL — used after Google redirect
FRONTEND_URL="http://localhost:3000"
