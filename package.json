{
  "name": "naman-workspace",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm run start:dev --prefix backend\" \"npm run dev --prefix web\"",
    "install:all": "npm install --prefix backend && npm install --prefix web && npm install --prefix mobile && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
